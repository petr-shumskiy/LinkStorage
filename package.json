{
  "name": "linkstorage",
  "version": "0.1.0",
  "description": "link-storage-app",
  "main": "app.js",
  "scripts": {
    "eslint": "npx eslint --fix . && cd client && npx eslint --fix ./src",
    "test": "echo \"there aren't tests yet\"",
    "start": "node index.js",
    "server": "nodemon index.js",
    "client": "npm run start-travis --prefix client",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "devTest": "timeout --preserve-status -s INT 20s concurrently 'npm run start' 'npm run client'"
  },
  "keywords": [
    "node",
    "mongodb",
    "react"
  ],
  "author": "Linkstorage team",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "config": "^3.3.2",
    "express": "^4.17.1",
    "mongoose": "^5.10.8"
  },
  "devDependencies": {
    "@craco/craco": "^5.8.0",
    "concurrently": "^5.3.0",
    "eslint": "^7.13.0",
    "eslint-config-standard": "^16.0.1",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-node": "^11.1.0",
    "eslint-plugin-promise": "^4.2.1",
    "nodemon": "^2.0.5"
  }
}
